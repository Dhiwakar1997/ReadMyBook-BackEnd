# CPU-only PyTorch (must be before surya-ocr/marker-pdf)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Azure Storage
azure-storage-queue==12.14.1
azure-storage-blob==12.27.1
azure-core==1.37.0

# Database
SQLAlchemy==2.0.45
psycopg2-binary==2.9.11

# Caching (billing uses Redis for balance cache)
redis==7.1.0

# Kafka
confluent-kafka>=2.3.0,<3

# PDF Processing
surya-ocr==0.15.2
marker-pdf==1.8.3
markdown-it-py==4.0.0
pypdf==5.1.0

# Utilities
python-dotenv==1.2.1
ulid-py==1.1.0
pydantic>=2.0.0,<3

# Mathpix fallback (when message fails 3 times)
httpx>=0.27.0,<1

# LLM (OpenAI via LangChain)
openai>=1.0.0,<2
qdrant-client==1.16.2
fastembed==0.7.4
tiktoken>=0.7.0,<1
