# Azure Storage
azure-storage-queue==12.14.1
azure-storage-blob==12.27.1
azure-core==1.37.0

# Database
SQLAlchemy==2.0.45
psycopg2-binary==2.9.11

# Caching (billing uses Redis for balance cache)
redis==7.1.0

# PDF processing (replaces marker-pdf + PyTorch + surya-ocr)
pymupdf>=1.27.1,<2
pypdf==5.1.0
markdown-it-py==4.0.0

# HTTP client (Mathpix fallback + SSE streaming)
httpx>=0.27.0,<1

# Utilities
python-dotenv==1.2.1
pydantic>=2.0.0,<3

# Vector DB (RAG)
openai>=1.0.0,<2
qdrant-client==1.16.2
fastembed==0.7.4
tiktoken>=0.7.0,<1
ulid-py==1.1.0
